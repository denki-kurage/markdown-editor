import { TokenEditorProps } from "../../markdown-block-editor/src/kurage/components/token-editor";

export const TableTokenEditor = ({ token, contexts }: TokenEditorProps) =>
{
    return (
        <div>TABLE EDITOR</div>
    )
}

