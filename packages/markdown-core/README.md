# Markdown Core

この機能はマークダウンエディタで使用する最低限の機能を提供します。
エディタごとにIAppContextインターフェースを実装し、MarkdownCoreクラスに渡すことでエディタとしての機能を提供します。

* 実際に動かすにはエディタ側で実装する必要があります。


サードパーティーのライセンスはルートディレクトリの`THIRD-PARTY-NOTICES.txt`ファイルにまとめられてます。

